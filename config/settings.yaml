# Mewgent settings
capture:
  window_title: "Mewgenics"
  interval_ms: 1000
  dpi_aware: true

ocr:
  gpu: true
  languages: ["en"]

database:
  path: "data/mewgent.db"

logging:
  level: "INFO"
  file: "mewgent.log"

debug:
  save_screenshots: true
  screenshots_dir: "debug_screenshots"
